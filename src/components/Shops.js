import React, { Component } from 'react'


export default class Shops extends Component {
    render() {
        return (
            <div>
                {/* <Link to={`${match.url}/shopId`}>
                    Example Shop Name 
                </Link>
                <Route path={`${match.path}/:shopId`} component={ShopContainer}/> */}
            </div>
        )
    }
}
